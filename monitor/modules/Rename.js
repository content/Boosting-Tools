import Module from "../classes/Module.js";

export default class Rename extends Module {
    constructor() {
        super();
    }
    
    metadata() {
        return {
            name: "Rename",
            description: "Renames all loaded bot accounts to match a specified username."
        }
    }

    init() {
        
    }

    run() {
        
    }
}